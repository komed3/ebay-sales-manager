extends ./_layout
include ./mixin/chart

append scripts
  script( src= "https://cdn.jsdelivr.net/npm/chart.js" )
  script( src= "https://cdn.jsdelivr.net/npm/chartjs-chart-sankey" )
  script( src= "/js/chart.min.js", defer )

block main
  div.box
    h3 Report für 
      q= report.label
    +chart( 'sankey', report.data )
